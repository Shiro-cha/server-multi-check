class Server:
    def __init__(self, hostname, ip_address, port,user, password):
        self.hostname = hostname
        self.ip_address = ip_address
        self.port = port
        self.user = user
        self.password = password